{% trans_default_domain 'FOSUserBundle' %}

{{ form_start(form, { 'action': path('fos_user_change_password'), 'attr': { 'class': 'fos_user_change_password' } }) }}
    {{ form_widget(form) }}
    <div class="row col-md-offset-4">
        <input type="submit" class="btn  text-xs-center" style="background-color: rgb(116, 158, 186) !important;" value="{{ 'change_password.submit'|trans }}" />
    </div>
{{ form_end(form) }}
